apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: {{.Chart.Name}}
    annotations: {}
spec:
    rules:
        - http:
              paths:
                  - path: /
                    backend:
                        serviceName: {{.Chart.Name}}
                        servicePort: 80